AI_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
AI_DEBUG=false

# Optional OpenAI fallback/provider settings
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-nano

# Notification delivery (log = simulate only, webhook = POST to gateway)
NOTIFY_DRIVER=log
NOTIFY_WEBHOOK_URL=
